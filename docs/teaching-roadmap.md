# 教学化改造路线图

本文件规划 GraphCalc 从"能看结论的可视化器"走向**教学工具 + 自学教材**的
分阶段任务.定位已确认:**教学侧优先,自学侧同步跟进**.

结论先写在最前面:**教学化的瓶颈不是渲染,是"过程不可见"与"题不在库里".**
项目的三维渲染,异步计算链路,五种积分法可视化都已经到位;真正挡住课堂的
是符号引擎只暴露最终结果,`example/` 没有加载器,默认场景对新手不友好.

- 面向学生/使用者的部分见 [第一部分](#第一部分现状解剖)到
  [第四部分](#第四部分教学要素设计);
- 面向自己/后来者学架构的部分见 [第五部分](#第五部分自学侧)与
  [第七部分](#第七部分风险与反模式);
- 每项任务用 `T` 编号,依赖关系与推荐执行顺序见
  [第三部分](#第三部分分阶段路线图).

## 工作量口径

本文用**半天**为工作量单位(理想连续投入,含自测,不含等待 CI).

| 记号 | 含义 |
| --- | --- |
| `0.5d` | 纯文档/配置,无逻辑改动 |
| `1–2d` | 单文件逻辑改动 + 测试 |
| `3–5d` | 跨层改动(TS + 可能 Rust),需要新增契约 |
| `1w+` | 触碰 Rust 符号引擎公开面,需要设计决策 |

---

## 第一部分:现状解剖

## 1.1 已经具备的教学资产

这些**不需要重建**,只需要重新组织.

| 资产 | 位置 | 教学价值 |
| --- | --- | --- |
| 11 个可运行场景 | `example/*.miko` | 直接可用,文件头注释已是教学级 |
| 场景功能对照表 | `example/README.md` | 已按"求导/偏导"与"其他主题"分类 |
| 求导用户文档 | `docs/derivatives-guide.md` | 8 节,含算子×对象可用矩阵 |
| 五种积分法可视化 | `src/render/visualization/integral/` | 黎曼(左/右/中)/梯形/辛普森/勒贝格 |
| 切线与切平面 | `src/render/core/renderers/AnalysisRenderer.ts` | 一阶导的几何意义直接可见 |
| 隐式场梯度 | `example/sphere_gradient.miko` | 梯度垂直于等值面,难得的直观演示 |
| 参数实时联动 | `src/compiler/dsl/params.ts` | 滑块驱动,适合课堂逐步演示 |
| 公式 KaTeX + 点击复制 | `src/ui/formula/FormulaView.ts` | 可做"结论对答案"环节 |
| 54 项自动化测试 | 354 (TS) + 129 (Rust) | 改坏的立刻知道,教学改造的安全网 |

## 1.2 数学表达力缺口

按"课堂会不会用到"排序,不按实现难度.

| # | 缺口 | 现状证据 | 教学影响 |
| --- | --- | --- | --- |
| G1 | **求导过程不可见** | `math_rs::symbolic_derivative(expr, var)` 只返回化简后的字符串 | 只能讲"结果是",讲不了"为什么";`derivative_rules.miko` 只能用并列对照代替推导 |
| G2 | **无极限 / ε-δ** | 语法与内核均无 `limit` 概念 | 导数的定义式,连续性,收敛都讲不了 |
| G3 | **无方程/不等式求解** | 无对应语句;`implicit` 只画等值线,不解 | 无法出"解不等式"类习题.求解的**过程展示**由 B5 先行,内核按保守式新增分期引入 |
| G4 | **函数族偏窄** | `src/math/math_rs/src/builtins.rs` 无 `floor`/`ceil`/`min`/`max`,无分段 | 分段函数,取整,最值问题全是盲区 |
| G5 | **体积对象不可求导** | `analyses.ts` 明确报"暂不支持 box/conic 体积对象" | `box`/`cone`/`cylinder` 的梯度演示缺席 |
| G6 | **`region` 只支持 x 型带** | `src/compiler/dsl/objects/build.ts` 文件头标注 V1 限制 | 极坐标区域,y 型区域,多边界区域无法演示 |

> **仍然不追求"完整 CAS".** README 已宣告"作为 CAS 是不完全的",这是定位
> 而不是缺陷.课程设计应顺着这个边界走:只讲能算的,把算不了的留成
> "下一版再说".**方程求解是唯一获准按"保守式新增"分期引入的例外**--先做
> 过程展示,再做几何联动,最后才谈内核;边界与理由见
> [方程求解过程的展示设计](equation-solving-process.md) 与第七部分.

## 1.3 教学动线缺口

比数学缺口更急,因为**成本低,收益直接**.

| # | 缺口 | 现状证据 | 影响 |
| --- | --- | --- | --- |
| D1 | **`example/` 没有加载器** | 全仓库 `grep` 不到任何 `.miko` 读取代码;`dist/assets` 里无 `.miko` | 学生必须手抄文件内容,离线状态下更是只能靠记忆 |
| D2 | **默认场景是杂物间** | `index.html` 的 textarea 混了 `param/point/vector/matrix/curve×2/integral/intersection`,还留着注释"临时示例,后续统一加入文档" | 首次打开认知过载,不知道从哪里开始 |
| D3 | **无练习模式** | 无题面/答案/判定机制 | 只能演示,不能练 |
| D4 | **无进度与引导** | 无课程入口,无步骤状态 | 学生不知道"下一步学什么" |
| D5 | **诊断只报错不解释** | `src/ui/diagnostics/DiagnosticsController.ts` 显示编译器错误 | 新手看到"引用不存在的对象"仍不知道改哪 |

---

## 第二部分:改造原则

写在这里是为了防止教学化改造变成新一轮重构爆炸.

1. **不碰编排层.** `src/app/DslApp.ts` 是编译/渲染/异步计算/UI 四条线的
   交汇点.教学改动一律走"新增示例素材 + 新增 UI 叶子组件 + 新增文档",
   不从编排层长出新分支.
2. **不改 `contract/ir.ts` 的既有字段语义.** 它是语言层与渲染层的唯一稳定
   契约.新增字段可以(如 A3 需要补充展示数据),改旧字段语义不行.
3. **不改 `src/wasm/*`.** 那是 `wasm-pack` 产物且被 `.gitignore` 忽略.
   内核行为变更一律改 `src/*_rs` 源码后重跑 `npm run build:wasm`.
4. **每个教学特性必须可单测.** 项目现有 46 个测试文件是安全网;新增
   逻辑若无法写成测试,说明它被放在了错误的层(大概率是塞进了 DOM 代码).
5. **UI 文案与数据分离.** 课程文案,学习目标,思考题都应放在纯数据文件
   (可参考 `src/config/uiConfig.ts` 的"只放纯数据"约定),不要硬编码进
   渲染逻辑,否则改一句文案要动代码.
6. **任何"要不要迁进 Rust"的判断必须有实测数字.** 口径见
   [docs/wasm-boundary-cost.md](wasm-boundary-cost.md):边界固定开销
   ~1.1 µs,`CompiledEvaluator` context 占求值成本 94–99%.凭感觉搬会亏.

---

## 第三部分:分阶段路线图

## 阶段 A:零内核风险(全部可只改文档/TS)

这一阶段的共同特征:**不触碰 Rust 内核,不需要重跑 WASM 构建**,因此
可以随时停,随时回滚.建议整段连做.

### T1 路线图入库与文档索引

| 项 | 内容 |
| --- | --- |
| 问题 | 教学化改造没有单一事实源,后续容易走散 |
| 改动 | 本文件落 `docs/`;在 `docs/README.md` 增一行索引;在根 `README.md` 的 `## meta` 前增一节"教学"指向本文件 |
| 依赖 | 无 |
| 工作量 | `0.5d` |
| 验收 | `docs/README.md` 能从索引跳到本文件;根 README 有回链 |
| 风险 | 无 |
| 降级 | 无 |

### T2 把 `example/` 改造成课程包

| 项 | 内容 |
| --- | --- |
| 问题 | 示例是"能跑"的素材,不是"能教"的教案:没有学习目标,没有观察指引,没有思考题 |
| 改动 | 为每个 `.miko` 统一文件头五段式:**学习目标 / 前置 / 操作 / 观察 / 思考**.现状文件头已含"本例演示什么"与部分数学解释,补齐"操作""观察""思考"即可.同步更新 `example/README.md` 的对照表,补一列"学习目标". |
| 依赖 | 无 |
| 工作量 | `2–3d`(11 个文件,每个 1–2 小时) |
| 验收 | 每个示例文件头五段齐全;`example/README.md` 表格与文件实际内容一致;所有示例仍能在页面里粘贴运行通过 |
| 风险 | **文件头注释里不要写"第 N 课"这类序数**--项目文件命名约定明确"不使用序号",序号属于教学编排,应放在 `README` 表格或未来的 UI 层,放错地方以后重排一次就要改 11 个文件 |
| 降级 | 只做求导主题的 5 个文件(`derivative_curve/rules/graph`,`partial_derivative_surface`,`sphere_gradient`),其余保持现状 |

> **文件名是否重命名?建议不改.** 现有文件名按主题命名,已被
> `example/README.md`,`docs/derivatives-guide.md`,根 `README.md` 多处交叉
> 引用.用 `example/README.md` 的表格顺序承载教学顺序即可,零引用成本.

### T3 补教学刚需但缺失的示例

`example/` 当前覆盖"求导/偏导/积分/求交/动画",缺**基础概念**类演示.

| 建议新增文件 | 覆盖概念 | 靠现有能力能否实现 |
| --- | --- | --- |
| `secant_to_tangent.miko` | 导数定义:割线->切线 | **能**.一个 `param h`滑块 + 点分析,拖动 h->0 |
| `limit_sequence.miko` | 极限的数值收敛 | **能**.`param n` 驱动表达式,观察结果列表逼近 |
| `direction_derivative.miko` | 方向导数 | **能**.`gradient` + `vector` 点积对比 |
| `gradient_ascent.miko` | 梯度上升/下降路径 | **能**.多点 + 向量场 |
| `implicit_contour.miko` | 二维隐式曲线 | 部分能.`implicit` 的 marching 渲染在 README 中标注为"留到后续",当前只有方程本体参与分析 |
| `taylor_approximation.miko` | 泰勒展开逐阶逼近 | **能**.多个 `derivative` 语句组合 |
| `polar_curve.miko` | 极坐标曲线 | **不能**(G6 / `curve` 是 y=f(x) 形式) |

| 项 | 内容 |
| --- | --- |
| 工作量 | `2–3d`(每个新增文件含调试) |
| 验收 | 新增文件均通过"粘贴到编辑器点运行"验证;数学结论与文件头注释一致(用 `integral`/结果列表交叉验证) |
| 风险 | **先验证再写注释**.项目内已有教训:注释里的数学结论错得比代码更隐蔽.每个新增示例的注释结论都应用符号求导或数值对拍确认 |
| 降级 | 只做 `secant_to_tangent.miko`.它是"导数定义"的唯一入口,单点收益最高 |

### T4 习题册

| 项 | 内容 |
| --- | --- |
| 问题 | 有演示无练习,学生无法自测 |
| 改动 | 新增 `example/exercises.md`:每题给出①题面②期望图像的文字描述③验证方法(拖哪个参数,结果列表该看到什么)④参考答案的 `.miko` 片段(折叠或置于文末) |
| 依赖 | T2(沿用五段式口径),T3(题的素材) |
| 工作量 | `1–2d` |
| 验收 | 每题都能用现有 DSL 写出答案并跑通;答案片段与题面数值一致 |
| 风险 | 出题超出 DSL 能力(G1–G6)会变成"无解题".**出题前先对照第二部分的缺口表** |
| 降级 | 每主题 2 题,共 8–10 题 |

### T5 示例加载器(UI 内课程入口)

| 项 | 内容 |
| --- | --- |
| 问题 | **D1,当前最硬的缺口**:`example/` 根本没进构建产物,学生只能手抄 |
| 改动 | ① `vite.config.ts` 增 `import.meta.glob('../example/*.miko', { query: '?raw' })` 或专用插件,把示例作为**字符串资源**打进产物;② `source code` 窗口标题栏增一个入口(落地成现在的"示例"按钮 + `#example-menu` 浮层;原文写的是已随窗口化删除的 `.panel-header`);③ 新增 `src/ui/ExamplePicker.ts` 纯逻辑组件,选中后写入 `#dsl-editor` 并触发 `run-btn` 的等价路径. |
| 依赖 | T2(需要示例带上标题元信息才能生成下拉项) |
| 工作量 | `2–3d` |
| 验收 | 下拉列出全部示例;选择后编辑器内容与文件一致;点击运行后场景正确;刷新页面后默认仍是原默认场景(不隐式改变启动行为) |
| 风险 | ① **PWA 预缓存**:`vite.config.ts` 的 `workbox.globPatterns` 只含 `js/css/html/ico/png/svg/jpg/gif/woff2/wasm`.若示例以独立文件形式产出,离线会取不到,需同步加扩展名或改为内联进 JS bundle(推荐后者,自动进预缓存).② **不要直接改 `DslApp`**,运行入口应由现有 UI 控制器触发 |
| 降级 | 只把 5 个求导示例内联进 `src/config/` 下的纯数据模块,先不做下拉,用按钮切换 |

### T6 默认场景简化

| 项 | 内容 |
| --- | --- |
| 问题 | **D2**:首次打开认知过载 |
| 改动 | 把 `index.html` 里 textarea 的内容换成最小可教场景(建议:单条 `curve` + 一个 `param` + 一个 `gradient` 点分析),原内容转为 T5 示例库中的一个条目(可命名 `tour.miko`). |
| 依赖 | T5(原内容必须有去处,否则等于删除功能) |
| 工作量 | `0.5d` |
| 验收 | 首次打开看到最小场景且一眼能懂;原默认场景可在示例库中找到并运行 |
| 风险 | **必须在 T5 之后做**.顺序反了会丢失现有默认场景里的求交/积分演示 |
| 降级 | 不换内容,只在编辑框顶部加一段 `//` 注释指引"想学什么请先看示例库" |

### T7 新手向诊断增强

| 项 | 内容 |
| --- | --- |
| 问题 | **D5**:错误只说"不合法",不说"怎么改" |
| 改动 | 在 `src/ui/diagnostics/DiagnosticsController.ts` 展示层增"常见原因 + 修法"映射表(纯数据模块),针对高频错误:引用不存在的对象,选项名拼错,`range` 参数个数与维度不符,`at` 坐标个数不对,`in cyclic` 写成 `in [..] cyclic`,`derivative` 引用了产物而非对象声明. |
| 依赖 | 无(可与 T2 并行) |
| 工作量 | `1–2d` |
| 验收 | 上述 6 类错误都能在诊断区看到修法提示;映射表有单测(输入错误消息 -> 期望提示) |
| 风险 | 提示写错比不写更坏.**映射表只覆盖编译期确定的错误**,不要试图解析任意错误字符串做正则猜测 |
| 降级 | 只做最高频的两类:引用不存在的对象,选项名拼错 |

### T8 步骤化求导的 TS 侧兜底(轻量版)

| 项 | 内容 |
| --- | --- |
| 问题 | G1,但完整方案见 B1(需要动 Rust) |
| 改动 | 在分析结果区除最终导数外,补一行**导数的定义式**(`f'(x₀) = lim_{h->0} [f(x₀+h) − f(x₀)]/h`)与**数值差商估算值**.数据来源:已有的 `evaluate_scalar` WASM 入口(两次求值),不新增内核接口 |
| 依赖 | 无 |
| 工作量 | `1–2d` |
| 验收 | 对 `example/derivative_curve.miko`,结果列表同时显示符号导数与 h 取小值时的差商,两者数值在容差内一致 |
| 风险 | **这只是"数值验证",不是"推导过程"**.不要把它宣传成步骤化求导,否则学生会以为看到了法则推导.命名上要区分清楚(建议 UI 文案写"差商校验") |
| 降级 | 不做.它价值有限,存在的意义主要是为 B1 探路 |

### T9 一页速查卡

| 项 | 内容 |
| --- | --- |
| 问题 | `docs/derivatives-guide.md` 是教程体,课堂/自测时需要的是"一页纸查语法" |
| 改动 | 新增 `docs/dsl-cheatsheet.md`:语句种类,对象种类,选项名,可用算子,维度约定,常见坑.表格式,追求可打印 |
| 依赖 | 无 |
| 工作量 | `0.5d` |
| 验收 | 覆盖 `miko.pest` 里全部语句类型与全部对象 kind;每条语法可在 `example/` 找到实例 |
| 风险 | 与 `derivatives-guide.md` 重复会双份维护.**速查卡只放语法,解释留在指南**,互相链接不复制 |
| 降级 | 无 |

> **阶段 A 完成后的状态**:学生打开页面即见最小示例,可从下拉选课,每课有
> 目标与思考题,有习题册,有速查卡,写错有修法提示.**全程未改一行 Rust.**

## 阶段 B:需要新增契约(TS 为主,少量 Rust)

### B1 步骤化求导(教学质量的分水岭)

| 项 | 内容 |
| --- | --- |
| 问题 | **G1,本项目从"可视化器"升级为"教具"的唯一关键项**.当前 `symbolic_derivative` 只给最终化简结果,求导法则(链式/积/商/幂)的应用过程被 `simplify` 吃掉了 |
| 现状证据 | `src/math/math_rs/src/symbolic/derivative.rs`,`simplify.rs`,`builtins.rs`(已实现完整的规则表:`derivative_sin/cos/tan/asin/...`,`derivative_log_base`,`expand_sec/csc/cot`,`expand_deg` 等),但 `pub(crate)` 级别,公开面只有字符串进出口 |
| 改动(四步,建议分 PR) | ①**Rust 侧新增产物类型** `DeriveStep { rule: &'static str, before: Expr, after: Expr }`,让求导递归在每一步记录所用法则;②**新增序列化**:把步骤链打印成 `Vec<String>`,每条形如 `d/dx[sin(u)] = cos(u)·du/dx (链式法则)`;③**新增 WASM 入口** `symbolic_derivative_steps(expr, var) -> Vec<String>`(独立函数,不改 `symbolic_derivative` 的签名,保住既有 116 个 Rust 测试);④**前端展示**:按 [方程求解过程的展示设计](equation-solving-process.md) 的**三级披露**落地--短过程留在条目 `<details>`(L1),长过程进右栏"过程"标签页(L2),KaTeX 渲染直接复用 `FormulaView`(LaTeX 能力 `symbolic/latex.rs` 已有).**不要**把任意长的步骤链直接铺进现有列表:那是本项最初的口径,已被 B5 取代--底栏一屏上限只有约 14 行,且与实体清单对半分宽 |
| 依赖 | T8(用于交叉验证数值正确性) |
| 工作量 | `1w+`,建议拆成 ①+②(内核,3–5d),③(边界,0.5d),④(UI,2–3d) |
| 验收 | 对 `sin(a*x)`,`x^2*exp(x)`,`ln(cos(x))`,`1/x` 至少四个案例,步骤链**每一步的首尾相接**(上一步的 `after` 等于下一步的 `before`),且最后一步等于现有 `symbolic_derivative` 的输出;Rust 侧新增逐案例断言测试 |
| 风险 | ① **`simplify` 会掩盖法则归属**:化简规则(如"同底数幂合并")与求导法则不是一个概念,步骤链中需明确区分"求导法则"与"代数化简"两类步骤,否则学生会把化简当求导.② **不要改 `Expr` 的公开性去图方便**,那会把符号引擎的内部表示泄漏成项目级 API.③ 步骤粒度是设计决策:是"每个函数一个步骤"还是"每次递归一个步骤",**先定粒度再写代码**,否则返工.④ 表达式树深度上限(`parser.rs` 的 `MAX_TREE_DEPTH`)会限制可展示的步骤数,超深表达式要么截断要么拒绝,需明确语义 |
| 降级 | 只对**一元复合函数**输出步骤(链式法则一条线),积/商法则仍只给结果.覆盖课堂 80% 需求而成本减半 |

### B2 极限演示(数值版,不碰符号)

| 项 | 内容 |
| --- | --- |
| 问题 | G2.导数定义式与收敛概念没有载体 |
| 改动 | 不新增语法.用现有 `param n` / `param h` + 结果列表 + `integral` 可视化组合出"分割加细 -> 积分"的演示;若要更直观,可在 `src/ui/view/controls/` 增一个"序列播放"控件,用 `AnimationPlayer` 已有的时间轴驱动参数变化(`src/render/core/AnimationPlayer.ts`) |
| 依赖 | T3(`limit_sequence.miko` 先证明可行) |
| 工作量 | `2–3d`(若只做示例 + 参数联动则 `0.5d`) |
| 验收 | 拖动参数时结果列表单调收敛且可停止;动画播放可暂停/重播 |
| 风险 | **不要新增 `limit` 语句**.符号极限需要级数/洛必达等一整套能力,是完整 CAS 的入口,会把项目拖进 G1 之外的深水区.数值演示已经够课堂教学用 |
| 降级 | 纯示例 + 参数滑块,不做动画控件 |

### B3 极坐标与更广定义域(按需)

| 项 | 内容 |
| --- | --- |
| 问题 | G6:`region` V1 只支持 x 型带;`curve` 只支持 y=f(x) |
| 改动 | 见 `src/compiler/dsl/objects/build.ts` 与 `src/render/core/renderers/RegionRenderer.ts` 文件头已写明的 roadmap:y 型区域 / 极坐标 r-θ 区域 / 多曲线边界 |
| 依赖 | 无,但**改动面涉及 IR 与渲染器** |
| 工作量 | `3–5d` |
| 验收 | 新增区域形态有单测;`contract/ir.ts` 的既有字段语义未变(只增枚举值/新字段) |
| 风险 | 这是**语言层扩展**,不是教学改造.只有在课程确实需要时才做.建议放在阶段 A/B 全部落地,确认真实需求后再评估 |
| 降级 | 暂不做,课程中回避极坐标区域 |

### B4 PWA 离线教学包

| 项 | 内容 |
| --- | --- |
| 问题 | 课堂网络不可靠;当前 `workbox.globPatterns` 未覆盖示例资源 |
| 改动 | `vite.config.ts` 的 `workbox` 段确认示例资源在预缓存范围内(推荐 T5 的内联方案,天然覆盖);核验 `maximumFileSizeToCacheInBytes`(现为 4MB)对新增资源仍够;全站当前 `dist` 为 4.5M,三个 wasm 约 1MB,余量充裕 |
| 依赖 | T5 |
| 工作量 | `0.5d` |
| 验收 | 断网后刷新页面,示例库可列出,可加载,可运行 |
| 风险 | 预缓存体积增长会影响首次加载.示例若内联进主 bundle,主 chunk(当前 1.04MB)会再涨,需实测首屏 |
| 降级 | 不做,课堂用在线版本 |

### B5 方程求解过程(右栏"过程"标签页)

| 项 | 内容 |
| --- | --- |
| 问题 | **G1 的展示侧 + G3 合并成的同一个教学需求**:学生要看到"每一步为什么".现状把过程挤在底栏求值条目的一个 `<details>` 里--底栏高上限 640px,扣掉标题与内边距后可用约 548px,简单递等式一行约 37px(带分式 55–70px),**一屏最多约 14 行**,且与实体清单对半分宽(默认 1920 屏每栏约 646px,长式子必然横滚) |
| 设计 | 见 [方程求解过程的展示设计](equation-solving-process.md):**不改浮层格局**,把右栏改成标签页(页1 参数/视图,页2 过程);过程页通高(1080p 下约 27 行),一行一步递等式,条目走三级披露(L0 摘要 / L1 行内短过程 / L2 过程页) |
| 一期(零内核风险) | 右栏标签页 + 过程视图 + 披露判据 + 条目"过程"入口.数据源是现有 `analysisLatexDetailEntries` 与积分/求交的细节行,**不动 Rust,不动 `contract/ir.ts` 既有字段,不新增 DSL 语句**.唯一要动的共享件是 `createObjectRow(rowClass, toggle)`:它只容得下一个行末按钮,需扩成"行末动作容器".**落地形态已变**:W6 把标签页/分隔条整条链路拆成六个独立窗口(`PanelController` / `RightPanelTabs` / `RightSplitController` 已删除),过程是独立窗口,详见 [窗口化计划](windowing-plan.md) |
| 二期 | 步骤索引驱动几何(割线->切线,黎曼矩形加细,交点高亮):把"第 k 步"当虚拟参数走 `CompileController.refresh` -> `RenderController.applyScene(scene, changedParams)`,复用既有缓存与 latest-only 调度;IR **只新增**字段(如 `stepHighlights`) |
| 三期 | 保守式求解内核:步骤产物用**独立类型**(不是 `Expr`,见 §7.1),配 WASM 入口.三期只换数据源:过程页骨架(标签页/递等式/披露)零改动;UI 增量只有"求解"子列表条目与过程页**题目区**--这正是展示层先行的价值 |
| 三期状态(v1 已落地) | 新增 `solve 名称 = 左 = 右 [选项];` 语句;**内核**在 `math_rs::symbolic/solve.rs`(+`poly.rs`),产物是独立的 `SolveOutcome`/`SolveStep`(只有字符串与计数,`Expr` 仍是 `pub(crate)`),WASM 入口 `solve_equation` 返回 JSON;TS 侧只**新增** `SolveTask`/`SceneIR.solves`(既有字段语义不变),渲染进"求解"子列表,过程页新增**题目区**.能力边界 v1:**单变量一次/二次多项式**,数值系数(参数按当前值代入);因式分解+零积律 / 判别式+求根公式两条路径;三次以上,多未知量,超越项明确报错.示例 `example/solve_equations.miko`,默认场景也带两条 `solve` |
| 依赖 | 一期无(可与 T8/B1 并行);二期原计划接一期预留的 `onStepChange` 出口--该出口已随翻步一起删除(见设计文档 4.4),二期需和真正的步骤交互一起重新设计接口;三期依赖 B1 的产物类型口径 |
| 工作量 | 一期 `3–5d`(跨 HTML/CSS/新控制器/共享行件契约),二期 `3–5d`,三期 `1w+`(需先豁免 §7.1 的禁令) |
| 验收 | 一期:①标签页切换后折叠/展开与 `dispose()` 复位与现有一致(**已作废**:标签页与 `PanelController` 已删除);②过程页激活时 `#params-panel` / `#right-splitter` 不参与布局,切回后分隔比例不变(**已作废**:过程是独立窗口);③长过程条目进过程页后底栏高度不变;④判据/步骤分区有纯函数测试;⑤`npm test` 与 `npm run typecheck` 全绿;⑥主 chunk 增量实测 < 30KB |
| 风险 | ①`PanelController._applyLayout()` 的折叠语义是"隐藏除承载按钮的 header 外的全部直接子元素",标签栏必须放进该 header 内部,页内容各包一层(**已作废**);②`RightSplitController.computeSplitRatio()` 以 `#right-panel` 矩形为基准,加标签栏后该前提失效,基准要换成页容器(**已作废**);③`--right-panel-width` 只有 `PanelController._applyLayout` 一个写入点(UI-P3.3 的教训),标签页控制器不能自己写变量(**已作废**,窗口宽度归 `WindowManager`);④右栏默认 300px 对递等式偏窄(实作后确认"按页记宽"会让切页时栏宽跳变,已改为参数页与过程页**共用一份宽度**,需要时手动拖);⑤参数被藏到另一页后用户可能忘了调参,过程页顶部保留只读参数回显 |
| 降级 | 只做一期且不做标签页:过程页做成右栏内一个可折叠区块,数据只接**积分与梯度**两类(细节行最多,递等结构最明显) |

## 阶段 C:长期/探索

### C1 练习判定

给定题面与学生的 `.miko` 代码,自动判定结果是否等价.**建议用"数值对拍"
而非符号等价**:在同一组采样点上比较两条曲线的函数值,容差内一致即通过.
符号等价判定是难问题,数值对拍 20 行代码就能做,而且与项目
`CompiledEvaluator` 的既有能力天然契合.

| 项 | 工作量 | 风险 |
| --- | --- | --- |
| `3–5d` | 容差与采样点选择会让判定出现假阴/假阳;需要"判定不确定"这个第三态,不要做二值判定 |

### C2 课程进度与持久化

| 项 | 工作量 | 风险 |
| --- | --- | --- |
| `1–2d` | 项目当前**刻意不使用 localStorage**(`src/config/uiConfig.ts` 明确"也不落 localStorage",唯一真相源是配置文件).进度持久化会打破这个约定,需先确认这是有意的策略还是历史遗留 |

### C3 步骤化积分的调研

先做**可行性调研**,不写实现.核心问题:现有积分是数值路径
(`math_rs::integral_core` 等),符号积分(原函数)是另一套完全不同的能力,
且符号积分不可判定(里施算法覆盖面有限).**调研结论大概率是"不做"**,
但把结论写进文档能避免以后重复讨论.

---

## 第四部分:教学要素设计

每个"课"建议统一四段,与 T2 的文件头口径一致.下面给可直接套用的模板.

## 4.1 单课模板

```text
// =============================================================
// <课名>:<一句话数学结论>
//
// 学习目标:<学完能做什么,动词开头>
// 前置:<需要先看哪个示例/哪个概念>
// 操作:拖动 <参数名>(<范围>),观察 <哪个几何元素>
// 观察:<应看到的定量/定性现象,含极限情形>
// 思考:<一道开放题,不给答案或答案在文末>
// =============================================================
```

## 4.2 按主题的教学顺序(建议)

沿用现有文件,不重命名.顺序由 `example/README.md` 的表格承载.

| 序 | 主题 | 现有/新增文件 | 核心结论 |
| --- | --- | --- | --- |
| 1 | 函数与参数 | `object_addition.miko` + 新增最小场景 | 表达式即图形,参数即滑块 |
| 2 | 导数定义 | 新 `secant_to_tangent.miko` | 割线极限 = 切线斜率 |
| 3 | 求导法则 | `derivative_rules.miko` | 积/商/链式的对照 |
| 4 | 导数函数图像 | `derivative_graph.miko` | f' 本身是函数 |
| 5 | 一元点分析 | `derivative_curve.miko` | 切线/法向的几何意义 |
| 6 | 偏导与切平面 | `partial_derivative_surface.miko` | 临界点处切平面水平 |
| 7 | 梯度与隐式场 | `sphere_gradient.miko` | 梯度垂直等值面 |
| 8 | 散度与旋度 | `divergence_vector_field.miko`,`curl_vector_field.miko` | 源汇 / 旋转 |
| 9 | 定积分与黎曼和 | 现有 `integral` 示例 + 五种方法对照 | 分割加细取极限 |
| 10 | 重积分与区域 | `double_integral_region.miko` | 积分域是平面区域 |
| 11 | 求交 | `intersection_line_curves.miko`,`intersection_surfaces.miko` | 方程的几何解 |
| 12 | 变换与动画 | `animation_box_rotations.miko` | 矩阵即变换 |

> 第 9 课是本项目**最有教学差异化优势**的一课:五种积分法的可视化在同类
> 工具里并不常见(`riemann:left/right/mid`,`trapezoid`,`simpson`,
> `lebesgue`).建议把重心放这里,并做成 `docs/` 里的重点文档.

## 4.3 习题设计约束

出题前逐条自查(防止出无解题):

- [ ] 表达式只用 `builtins.rs` 里有的函数(没有 `floor`/`min`/`max`/分段)
- [ ] 求导题不含 `abs` 的不可导点(有 `sign`,但语义是弱导数)
- [ ] 区域题只用 x 型带(`region` V1 限制)
- [ ] 求导题不引用 `derivative` 的产物(对象相加明确不支持)
- [ ] 体积对象的梯度题不用 `box`/`cone`/`cylinder`/`frustum`(G5)
- [ ] 数值积分题给出方法名与分段数(不同方法结果不同,避免答案争议)

---

## 第五部分:自学侧

定位确认"两者都要",自学侧与教学侧共用大部分产出.以下按"先做哪个"排序.

## S1 端到端导读(自学门槛最高的一项)

现有根 `README.md` 的"架构"一节是**结论**("四条线拼在一起"),缺一条
**跟着一条曲线走完全程**的追踪:从 textarea 的一个字符到屏幕上的一个像素.
建议新增 `docs/end-to-end-tour.md`,覆盖:

1. `index.html` -> `src/main.ts` -> `applyUiConfig()` 为何必须在 `DslApp` 之前
   (注释已写明"晚一步就会量到兜底字体")
2. `DslApp.run()` -> `CompileController` 的**运行序号**机制(为何过期结果要丢弃)
3. `parseMiko()` 的 WASM 边界:输入字符串,输出 `AstProgram` 的形状
4. `DslCompiler.compileScene()` 的五个子步骤与各自缓存
   (`getOrBuildStaticScene` 缓存什么,为什么参数滑块不重新解析源码)
5. `SceneIR` 的字段与"表达式只存字符串"这个决定的代价
6. `RenderController` 如何按 `coefficients` 判断哪些对象需要重采样
7. Worker 消息格式与 `LatestRequestExecutor` 的 `superseded` 语义
8. 渲染器路由(`src/render/core/Plotter.ts` 的 id -> renderer 映射)

| 项 | 工作量 | 验收 |
| --- | --- | --- |
| `2–3d` | 每一步都有可点击的源码行号引用;读者能据此独立新增一个渲染器 |

## S2 把性能实测文档提到顶层

`docs/wasm-boundary-cost.md` 记录了"什么该迁 Rust,什么**不该**迁"与实测
数字(边界固定开销 ~1.1 µs;`CompiledEvaluator` context 占求值成本 94–99%).
这类"测量驱动决策"的案例在开源项目里稀缺,但现在埋在 `docs/` 索引第三行.
建议在根 `README.md` 的"架构"一节内用一段话点出它,并链过去.

| 项 | 工作量 |
| --- | --- |
| `0.5d` |

## S3 踩坑速查表

把散落在代码注释里的教训汇成一页 `docs/pitfalls.md`.已确认的高价值条目:

- `src/wasm/*` 是产物,`grep` 到它不代表可以用它(根 README 有强警告)
- 本地 dev 与 `dist` 的差异:`base: '/miko_graphcalc/'` 是唯一路径开关
- 测试环境里 wasm-bindgen 默认初始化走 `fetch(file://)` 会失败,故
  `vite.config.ts` 配了 `setupFiles: ['./src/testing/setupWasm.ts']` 用
  `initSync` 从磁盘读 `.wasm`
- 全角标点被 `.githooks/pre-commit` 自动转半角;hook 自身必须用半角书写
  (否则每次提交都产生无意义 diff)
- `jacobian` 能被语法解析但编译期报"暂未实现",向量场的逐分量拉普拉斯
  `∇²F` 同样只报"暂不实现"--**语法接受 ≠ 功能存在**,看 `miko.pest`
  不能判断能力边界(`laplacian` 的标量场形态已实现)
- 隐藏语句的语义是"先完整校验,后禁用,仅跳过数值计算"
  (`analyses.ts` 头部注释),不是"跳过一切"

| 项 | 工作量 |
| --- | --- |
| `1d` |

## S4 测试驱动教程

354 + 129 个测试是活的规格说明.建议以"新增一个对象类型"为主线写
`docs/contributing-tour.md`:先写 `DslCompiler.test.ts` 的失败用例,再依次
补 `ast/types.ts` -> `miko.pest` -> `parser_wasm.rs` -> `build.ts` ->
`contract/ir.ts` -> `renderers/` -> `Plotter.ts`.这条主线走完一遍,就把整个
编译链路走通了.

| 项 | 工作量 | 前置 |
| --- | --- | --- |
| `2–3d` | S1(需要导读建立的全局观) |

---

## 第六部分:推荐执行顺序

```text
阶段 A(全部零内核风险,建议连做)
  T1 路线图入库 ─┬─ T2 课程包 ─┬─ T4 习题册
                 │             └─ T5 示例加载器 ── T6 默认场景简化
                 ├─ T3 补示例 ── B2 极限演示(数值版)
                 ├─ T7 诊断增强
                 ├─ T8 差商校验 ── B1 步骤化求导
                 └─ T9 速查卡
                        │
阶段 B ─────────────────┴─ B4 离线包(依赖 T5)
                           B3 极坐标(按真实需求决定做不做)
                           B5 方程求解过程(一期可独立开工,不必等 B1;
                              二期起依赖 B1 的步骤产物口径)

自学侧(与阶段 A 并行,共用素材)
  S2 性能文档提级(最省力) ── S1 端到端导读 ── S4 测试驱动教程
  S3 踩坑速查表(可与任何一项并行)
```

**如果只能做三件事**:`T2`(课程包),`T5`(示例加载器),`B1`(步骤化求导).
前两项让项目"能被学",第三项让它"能教过程".其余都是锦上添花.

---

## 第七部分:风险与反模式

## 7.1 明确不要做的事

| 反模式 | 为什么 |
| --- | --- |
| **补全 CAS**(符号积分,极限,级数) | README 已把"不完全"定为定位.补全没有终点,且会稀释已有的可视化优势.**2026.09 受控豁免**:不定积分(原函数)与可解类型清单内的微分方程已获准按"可回代验证 + 明确能力边界"的保守口径新增,边界与分期见 [不定积分与微分方程:设计计划](calculus-suite-plan.md);极限,级数,特殊函数仍然不做 |
| **跳过展示层直接写求解器** | 方程求解本身按"保守式新增"分期引入(见 B5);但把它当成"多支持一种方程类型"来做,等于用一个 parser + solver 换掉了真正的缺口--**过程不可见**.顺序必须是:先展示,再联动,最后内核 |
| **新增 `limit` 语句** | 是完整 CAS 的入口.数值演示(阶段 B2)已满足课堂教学 |
| **改 `src/wasm/*`** | 产物且被 gitignore;改动会被下次构建覆盖 |
| **在 `DslApp` 上继续长功能** | 它是四条线的交汇点,教学特性在此堆积会重现项目早期"看不懂"的问题 |
| **改 `contract/ir.ts` 既有字段语义** | 语言层↔渲染层的唯一稳定契约,语义漂移会同时影响所有渲染器与测试 |
| **为教学给 `Expr` 加公开 API** | 会把符号引擎内部表示泄漏成项目级 API(B1 的步骤产物应是独立类型,不是 `Expr`) |
| **做"二值"练习判定** | 数值对拍必然有边界情况,必须留"判定不确定"第三态 |
| **未经测量就把计算迁进 Rust** | 口径见 `docs/wasm-boundary-cost.md`;边界固定开销 ~1.1 µs 在逐点调用场景会主导成本 |

**受控豁免口径注记(三期:微分方程)**.三期沿用同一条口径,**不新增例外类别**:

- **内核能力边界走结果字段,绝不用异常表达"超纲"**.超出可解清单(二阶非齐次,
  二阶变系数/可降阶,`y'` 非线性等)时,`solve_ode` 返回 `Ok(OdeOutcome)`,理由
  放进 `error`,TS 侧转为 `OdeTask.error` 并在"微分方程"子列表**保留占位**;
  `Err`/编译期异常只留给"方程读不出来,初值写错,含未声明符号"这类真正的输入
  错误.一阶方程只要 `y'` 是一次式,即使超出可解清单也照常下发斜率场,学生至少
  能看见方程的斜率分布.
- **展示层如实标注,不替内核圆场**.隐式解明确写成"隐式解(`Φ(x,y) = C`),不是
  `y = ...`",并说明未下发解曲线;未定出常数,缺省自变量,未显式化等都由细节区
  照实回显.
- 依据:**三期完整规划与设置**(原文已按"过期文档"删除,可从
  `git show 6ad16b0^:docs/plan3.md` 取回)第 1.2/1.4 节与第 7 节第 5 条.

## 7.2 隐性风险

1. **文档双份维护**.教学文案一旦同时出现在 `README`,`docs/`,文件头注释
   三处,必然漂移.约定:文件头放"这一课讲什么",`docs/` 放"为什么这样",
   `README` 只放索引与回链,不复制内容.
2. **注释里的数学错误比代码错误更隐蔽**.项目已有高质量注释,但教学改造
   会新增大量数学断言,每条都应可用数值或符号对拍验证.
3. **首屏体积**.主 chunk 已 1.04MB,全站 4.5M.示例内联,步骤展示,KaTeX
   步骤行都会加码,每次新增都要实测首屏.
   **实测记录**:B5 一期(右栏标签页 + 过程视图 + 披露判据 + 行末动作容器,
   只加 DOM 与文本,不加依赖)主 chunk `index-*.js` 由 1097205 字节增至
   1107745 字节(**+10.3KB**,gzip 306.0KB);样式表由 49345 字节增至
   54016 字节(+4.6KB).均在 30KB 预算内(见
   [方程求解过程的展示设计](equation-solving-process.md) 第 8 节).
   **实测记录(三期求解内核 v1)**:`solve` 语句 + Rust 内核 + "求解"子列表
   - 过程页题目区,主 chunk 由 1107745 字节增至 1114489 字节(**+6.6KB**,
   gzip 308.2KB);样式表 54016 -> 54515 字节(+0.5KB).内核本体在 WASM 资产:
   `math_rs_bg.wasm` 由 413.51kB 增至 445.05kB(**+31.5KB**,gzip 165.8 -> 177.1kB),
   `compiler_rs_bg.wasm` 由 228.14kB 增至 232.33kB(+4.2KB,新增 `solve_stmt` 语法).
   "主 chunk 增量"仍远低于 30KB;内核增长发生在 WASM 资产上,已如实记录.
   **实测记录(不定积分一期:内核 + 原函数对象下发 + 求值子列表)**:主 chunk
   `index-*.js` 由 1114489 字节增至 1126212 字节(**+11.7KB**,gzip 311.4KB);
   样式表 54515 -> 54633 字节(+0.1KB);`compiler_rs_bg.wasm` 由 232.33kB 增至
   304.59kB(+72.3kB,新增 `antiderivative_stmt` 语法与产物 facts 序列化);
   `math_rs_bg.wasm` 由 445.05kB 增至 638.04kB(**+193.0kB**,gzip 212.3kB)--
   这是初等积分内核(表 / 换元 / 分部 / 有理函数部分分式 / 回代验证)的真实
   体积代价,远大于求解内核.若需压回,可行的方向是给 `math_rs` 开
   `[profile.release] lto = true` 与 `opt-level = "z"` 并实测收益;
   本期按"先确认功能,再谈体积"处理.
   **实测记录(三期:微分方程)**:`ode` 语句 + 内核(`symbolic/ode.rs`)+
   "微分方程"子列表 + 斜率场/解族实体下发,主 chunk `index-*.js` 由 1126212
   字节增至 1144923 字节(**+18.3KB**,gzip 311.4 -> 317.33kB);样式表 54633 ->
   54718 字节(+0.1KB);`compiler_rs_bg.wasm` 由 240044 字节增至 246584 字节
   (+6.4KB,新增 `ode_stmt` 语法与初值切分);`math_rs_bg.wasm` 由 514308 字节
   增至 604933 字节(**+88.5KB**,gzip 237.55kB)--这是五类一阶 + 二阶常系数
   齐次内核(记号归一 / 分类判定 / 积分器复用 / 初值定常数 / 回代验证)与
   `OdeOutcome` 序列化的真实代价;`render_rs_bg.wasm` 同步 +88.5KB(它复用
   `math_rs` 的符号模块).主 chunk 增量仍在 30KB 预算内(见
   [方程求解过程的展示设计](equation-solving-process.md) 第 8 节);WASM 增量
   远大于求解/不定积分两期,四期 4.2 的 `lto`/`opt-level = "z"` 实测因此更有
   必要.
4. **顺序依赖 D2->D1**.简化默认场景必须晚于示例加载器,否则会丢失现有
   默认场景的演示能力.
5. **阶段 A 的"零风险"只对内核成立**.T5/T6 会动 `index.html` 与 UI 叶子
   组件,仍需跑 `npm test` 与手工验证页面.
6. **右栏一旦多用途,布局状态就有第二处来源**.B5 把右栏改成标签页后,面板
   折叠,分隔条比例,栏宽三件事同时被"哪一页在前"影响.缓解只有一条:
   每个状态保留**唯一写入点**.这条教训在窗口化时以另一种形式保留了:
   窗口几何/状态/z-order 各自只有一个写入点(`_applyGeometry` / `_applyState` /
   `focus()`),任何"顺手写一下 CSS 变量或类名"的捷径都会重现 UI-P3.3 那类
   自相矛盾的面板.历史(折叠/栏宽归 `PanelController`,比例归
   `RightSplitController`,页归属归标签页控制器)见
   [方程求解过程的展示设计](equation-solving-process.md) 第 3.2 节.

## 7.3 每项任务的通用验收清单

- [ ] `npm test` 全绿(46 个文件 / 354 tests)
- [ ] `npm run typecheck` 通过(`--noUnusedLocals --noUnusedParameters`)
- [ ] 若动了 Rust:`cargo test --workspace` 全绿(129 tests)且
      `npm run lint:rs` 通过
- [ ] 若动了 Rust 源码:已重跑 `npm run build:wasm` 并验证页面实际生效
- [ ] 新增逻辑有对应单测,或说明了为何不可测
- [ ] 未修改 `src/wasm/*`,未改 `contract/ir.ts` 既有字段语义
- [ ] 中文文案提交后标点为半角(由 pre-commit hook 自动处理)

---

## meta

本路线图基于 2026.09 工作区实况撰写:实测 `npm test` 46 文件 354 通过,
`cargo test --workspace` 129 通过(1 ignored);核实 `example/` 无加载器且
未进 `dist`;核实 `symbolic_derivative` 为唯一符号求导入口且只返回字符串.
工作量数字是相对估计而非承诺,执行前应按当时的代码状态复核.
