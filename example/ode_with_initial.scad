// =============================================================
// 微分方程:初值把解族收成一条(族 vs 特解)+ 隐式解
//
// 学习目标:看懂"通解是一族曲线,初值从族里挑出一条特解";并认识隐式解
//   为什么不下发解曲线.
// 前置:先看 `ode_separable.scad`(可分离方程的分离变量与积分)与
//   `ode_second_order.scad`(特解由初值定出).
// 操作:在对象列表里开关 `I1_c2` 与 `I1_p`,确认两条曲线完全重合;
//   再打开 `I1` 的过程页,看"代入初值"那一步.
// 观察:
//   1) `I1` 一条语句同时写了 `curves = 3` 与初值 `y(0) = 1`:
//      `I1`    -> 斜率场 `surface z = x*y`(方程右端),
//      `I1_c1` -> 族里 C = 0 的那条(水平直线 y = 0),
//      `I1_c2` -> 族里 C = 1 的那条,`I1_c3` -> C = -1 的那条,
//      `I1_p`  -> 特解:初值 `y(0) = 1` 把 C 定成 1,所以 `I1_p` 与 `I1_c2`
//                 恰好是同一条曲线--**特解就是族里过点 (0, 1) 的那一个解**;
//   2) 族(通解)含未定常数 C,是"所有解"的集合;特解把 C 定死,是"过给定
//      初值点的那一个解".过程页会分别给出通解与特解,并在最后回代验证;
//   3) `I2 = y' = 1/(y^2+1)` 是另一类可分离方程:分离变量后要对 y 积
//      `y^2+1`,得到 `y^3/3 + y = x + C`--这是个三次方程,解不出 `y = f(x)`,
//      所以内核给的是**隐式解**,并且**不下发解曲线**,只下发斜率场
//      `surface z = 1/(y^2+1)`(右端只含 y,所以曲面沿 x 方向是一条条水平
//      条带,高度随 |y| 增大而衰减).过程页会把通解明确标注为
//      "隐式解(Φ(x,y) = C),不是 y = ... 的显式形式".
// 思考:为什么 `I1_p` 与 `I1_c2` 完全重合,而 `I1_c1`(C = 0)不重合?
//   若把选项 `constant` 改成 2,`I1_c1` 会变成哪一条?
// =============================================================

ode I1 = y' = x*y, y(0) = 1 {
    curves = 3;
    range = [-2, 2, -2, 2];
}

// 隐式解:只有斜率场,没有解曲线
ode I2 = y' = 1/(y^2+1) {
    range = [-3, 3, -2, 3];
}
