// =============================================================
// 微分方程:二阶常系数齐次 a*y'' + b*y' + c*y = 0(三种根型)
//
// 学习目标:看懂特征方程 a*λ² + b*λ + c = 0 的三种根型(两相异实根 / 二重根 /
//   共轭复根)分别给出什么样的通解,以及初值怎样从通解里定出一条特解.
// 前置:先看 `ode_separable.miko` 与 `ode_linear_first_order.miko`(对象
//   命名口径:语句名是主对象/斜率场,`_cN` 是解族曲线,`_p` 是特解曲线).
// 操作:分别打开 `R1` / `R2` / `R3` 的过程页,对照下面列出的特征根算术;
//   再在对象列表里开关 `R1_p`,看初值挑出的那一条与两条族曲线的关系.
// 观察(本示例三条语句各覆盖一种根型):
//   1) `R1: y'' - 3*y' + 2*y = 0` -> 特征方程 λ² - 3λ + 2 = (λ-1)(λ-2) = 0
//      -> **两相异实根** λ = 1, 2,通解 `y = C_1*e^x + C_2*e^(2x)`;
//      解族 `R1_c1 = e^(2x)`(取 C_1=1, C_2=0),`R1_c2 = e^x`(取 C_1=0,
//      C_2=1).这条语句另写了两个初值 `y(0) = 0`,`y'(0) = 1`,定出特解
//      `R1_p: y = e^(2x) - e^x`(C_1=1, C_2=-1;在 x=1 处约为 4.67);
//   2) `R2: y'' - 2*y' + y = 0` -> 特征方程 λ² - 2λ + 1 = (λ-1)² = 0
//      -> **二重实根** λ = 1(重根),通解 `y = (C_1 + C_2*x)*e^x`
//      --第二个基解要额外乘一个 x;`R2_c1 = e^x`,`R2_c2 = x*e^x`;
//   3) `R3: y'' + y = 0` -> 特征方程 λ² + 1 = 0 -> **共轭复根** λ = ±i
//      (实部 0,虚部 1),通解 `y = C_1*cos(x) + C_2*sin(x)`;
//      `R3_c1 = cos(x)`,`R3_c2 = sin(x)`.
//   三条语句都只有解曲线,没有斜率场实体--斜率场只对一阶方程
//   `y' = f(x,y)` 有意义,二阶方程不下发 `surface`;同一条语句下的解族共享
//   一种颜色,按对象名(`_p` / `_cN`)区分.
// 思考:`R1_p` 与两条族曲线 `R1_c1`,`R1_c2` 是什么关系(提示:通解是
//   `C_1*e^(2x) + C_2*e^x`,初值只是给 C_1,C_2 定数)?非齐次的
//   `y'' + y = sin(x)` 超出三期清单(二阶只支持常系数齐次),在列表里会走
//   "能力边界"并给出理由.
// =============================================================

ode R1 = y'' - 3*y' + 2*y = 0, y(0) = 0, y'(0) = 1 {
    curves = 2;
    range = [-1, 1];
    color = "#6dd5ff";
}

ode R2 = y'' - 2*y' + y = 0 {
    curves = 2;
    range = [-2, 2];
    color = "#6bffb8";
}

ode R3 = y'' + y = 0 {
    curves = 2;
    range = [-3, 3];
    color = "#fb923c";
}
