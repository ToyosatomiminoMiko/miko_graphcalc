// =============================================================
// 微分方程:可分离方程 y' = x*y
//
// 学习目标:看懂 `ode 名称 = 方程 [, 初值...] [选项];` 一条语句如何同时下发
//   "斜率场 + 解族 + 特解"三类对象,以及可分离方程的解法主线.
// 前置:先看 `antiderivative_basic.miko`(积分与回代验证)与
//   `solve_equations.miko`(编译期符号求解的过程页怎么读).
// 操作:在底部"微分方程"子列表点条目行末的"过程",右栏过程页会从"分离变量"
//   一路排到"回代验证";再在对象列表里逐个开关 `O1` / `O1_p` / `O1_c1...c3`,
//   分别看斜率场,特解,解族长什么样.
// 观察:
//   1) 解法:y' = x*y 可分离,dy/y = x dx,两边积分得 ln|y| = x²/2 + C,
//      于是通解是 `y = C*e^(x²/2)`;
//   2) 对象命名口径(全项目统一):
//      `O1`    -> 斜率场 `surface z = x*y`,即方程右端 f(x,y);曲面的高度/
//                 颜色就是该点斜率的大小(它是"高度图",不是箭头场),
//      `O1_c1` -> 解族曲线,常数 C = 0(选项 `constant` 缺省就是 0),
//      `O1_c2` -> 解族曲线 C = 1,`O1_c3` -> C = -1(`curves = 3`),
//      `O1_p`  -> 特解曲线:初值 `y(0) = 2` 把 C 定成 2;
//   3) 形状:因为指数上带 x²,族曲线关于 y 轴对称--C > 0 是开口向上的
//      "碗形",C < 0 是它的镜像,C = 0 是水平直线 y = 0(平衡解).它比普通
//      指数 e^x 增长更快(C = 1 的那条在 x = ±2 处已约 7.4).注意本示例不是
//      逻辑斯蒂型 S 曲线:那种 S 形来自 `y' = y*(1-y)` 一类的方程,不是这里
//      的 `y' = x*y`.
// 思考:`O1_p`(C = 2)与哪条族曲线一样关于 y 轴对称?把 `curves` 改成 4,
//   新出现的 `O1_c4` 常数 C 取多少,它和 `O1_p` 是什么关系?
// =============================================================

ode O1 = y' = x*y, y(0) = 2 {
    curves = 3;
    range = [-2, 2, -2, 2];
}
