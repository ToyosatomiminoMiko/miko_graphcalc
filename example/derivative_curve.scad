// =============================================================
// 一元函数求导:y = f(x) 在一点的导数,切线与法向
//
// DSL 目前没有独立的"求导"关键字:对 curve 写 gradient 分析,
//     gradient g = grad(c) at [px, 0];
// 就等价于求 y = f(x) 在 x = px 处的一阶导数--表达式在编译期由
// 符号引擎对 x 求导(链式/积/商法则),运行时把 px 代入求值.画面上:
//   - point   :曲线上的分析点 (px, f(px), 0)(黄色圆点);
//   - tangent :过分析点的切线(绿色直线),方向 = (1, f', 0),
//              即 Δx 走 1,Δy 走 f' 的直线,斜率 = f'(px);
//   - normal  :切线的法向箭矢(红色),方向 = (-f', 1, 0) 归一化--
//              正是隐式曲线 y - f(x) = 0 的梯度方向,把切线方向
//              (1, f', 0) 逆时针转 90° 得到:f' > 0 时箭矢斜向左上,
//              f' < 0 时斜向右上,f' = 0 时竖直向上.
//
// 一元 curve 求导不写 show 时默认画 [point, normal, tangent];
// 想只看部分元素时再显式写 show(见 derivative_rules.scad 里
// show = [point, tangent] 的用法).
//
// 本例 f(x) = sin(a * x):
//     f'(x) = a * cos(a * x)
// 拖动 a(角频率)或 px(观察点),绿色切线始终贴住曲线转动,
// 红色法向箭矢始终与切线垂直;右侧"结果"列表打印 f(P) 与 ∇f.
// =============================================================

// 可调参数:角频率 a,观察点 x 坐标 px
param a = 1 in [0.2, 4, 0.1];
param px = 0.8 in [-6, 6, 0.1];

// 曲线 y = sin(a * x);符号引擎求出的导数为 a * cos(a * x)
curve c = sin(a * x) {
    color = "#6dd5ff";
    range = [-8, 8];
    segments = 256;
};

// 不写 show:curve 求导默认画 [point, normal, tangent]
gradient g = grad(c) at [px, 0];
