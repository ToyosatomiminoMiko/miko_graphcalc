// =============================================================
// 微分方程:一阶线性 y' + p*y = q(积分因子 + 参数联动)
//
// 学习目标:看懂一阶线性方程的标准形与积分因子解法,并亲眼确认"参数在解里
//   保持符号"--拖动 p / q 时解式,斜率场,解族一起重算,不会冻在旧值上.
// 前置:先看 `ode_separable.miko`(对象命名口径:`L1` 是斜率场,`L1_c1...`
//   是解族,`L1_p` 是特解).
// 操作:拖动参数滑块 `p` 与 `q`,看斜率场平面怎么倾斜,解族怎么整体移动;
//   再把过程页里"积分因子"那一行的表达式与视口里的曲线对照.
// 观察:
//   1) 解法:y' + p*y = q 是一阶线性,积分因子 μ = e^(∫p dx) = e^(p*x),
//      于是 (μ*y)' = μ*q,两边积分得通解 `y = q/p + C*e^(-p*x)`(p ≠ 0).
//      过程页会依次给出"化为标准形 -> 积分因子 -> 两边积分 -> 回代验证";
//   2) 参数联动:解式与斜率场里 `p`,`q` 保持符号,数值由物化层按当前滑块
//      折叠,所以拖动滑块曲线跟着重算:把 `p` 加大,衰减更快,更贴近水平线;
//      把 `q` 加大,整族向上平移.斜率场 `L1 = surface z = q - p*y` 同时变;
//   3) 解族口径:`L1_c1`(C = 0)是水平直线 `y = q/p`--方程的**平衡解**;
//      `L1_c2`(C = 1)与 `L1_c3`(C = -1)是分别从上方,下方趋近它的两条
//      曲线(p > 0 时沿 x 正方向衰减到 q/p,沿负方向发散).
// 思考:把 `q` 拖到 0,方程变成 y' = -p*y,解族长什么样?此时 C = 0 的那条
//   曲线还在吗?
// =============================================================

// 可调参数:系数 p 与右端 q(区间 [min, max, step] 必须合法)
param p = 2 in [0.5, 3, 0.1];
param q = 1 in [0, 3, 0.1];

ode L1 = y' + p*y = q {
    curves = 3;
    range = [0, 3, -2, 4];
}
